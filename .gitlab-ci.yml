stages:
  - check
  - publish

check:
  image: node:latest
  stage: check
  script:
    - make dependencies
    - make lint

publish:
  image: node:latest
  stage: publish
  only:
    - tags
  script:
    - make dependencies
    - make lint
    - npm install -g vsce
    - vsce publish -p "${GITLAB_VSCE_PAN}"
